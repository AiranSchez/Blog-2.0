---
// ParallaxSection disabled for performance
// Simply acts as a passthrough wrapper now
interface Props {
  speed?: number;
  delay?: number;
}

const { speed = 0.3, delay = 0 } = Astro.props;
---

<div>
  <slot />
</div>
